# private/ — ignored, and deliberately so

This repository is public. Brokerage exports, account statements, 1099-Bs and
trade history go in this directory and are excluded by `.gitignore`.

Only this README is tracked. If you ever find a CSV of your own trades staged
for commit, the answer is not to write a better commit message.

Analysis code belongs in `src/`; the data it reads belongs here.
